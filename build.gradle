plugins {
	id 'base'
}

allprojects {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
	}
}

tasks.named('build') {
	dependsOn ':minecraft-progress-show:build', ':better-pet:build'
}
